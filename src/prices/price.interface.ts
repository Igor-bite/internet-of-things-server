export interface Price {
  id: number;
  planName: string;
  priceForMonth: number;
  devicesMax: number;
  usersMax: number;
  widgetsTypes: string;
  logsStorageTime: string;
  buttonActionText: string;
}