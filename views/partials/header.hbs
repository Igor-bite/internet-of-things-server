<div class="navbar">
  <div class="menu">
    <h3 class="logo">Corner of <span>IoT</span></h3>
    {{#if (isdefined isLogged)}}
      {{#if isLogged}}
        <h2> Your logged in! </h2>
      {{else}}
        <div>
          <a href="projects"><button class="custom-btn btn-16">Sign In</button></a>
          <a style="margin-left: 10px" href="prices"><button class="custom-btn btn-16">Sign Up</button></a>
        </div>
      {{/if}}
    {{/if}}
    <div class="hamburger-menu">
      <div class="bar"></div>
    </div>
  </div>
</div>